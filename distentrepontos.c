#include "stdio.h"
#include "stdlib.h"
#include "math.h"

int dist(){

    int X[10],Y[10],i, dis,j;

    dis= sqrt(pow((X[i]-X[j]),2) + pow((Y[i]-Y[j]),2));


    return (dis);
}
int main (){

    int X[10], Y[10], i, j, k, l, Distancias[10];

    for(i=0;i<10;i++)
        scanf("%d %d", &X[i], &Y[i]);

    for(i=0,j=0,k=1,l=1; k<10; k++,l++){


    }



}
